#include "Patient.h"



int main(){
    Patient* patients = new Patient[10];
    int count = 0;
    while (count < 10)
    {
        std::cin: name, surname, patronymic;
        std::cin:born_date;
        // Положить в массив
        count++;
    }
    
    return 0;
}